# Emacs major mode for SAS on Windows

This code in this repository provides a major mode for editing SAS program files and interacting with SAS assuming the separate existence of a program on the system to allow interactive SAS sessions through the command line.